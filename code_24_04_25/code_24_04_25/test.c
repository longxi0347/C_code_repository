#define _CRT_SECURE_NO_WARNINGS

/*
//leetcode 4月25日每日一题 2739
#include <stdio.h>
int main()
{
	int mainTank = 0;
	int additionalTank = 0;
	scanf("%d %d", &mainTank, &additionalTank);
	int cost = 0;
	int distance = 0;
	while (mainTank != 0)
	{
		cost++;
		mainTank--;
		distance += 10;
		if (cost % 5 == 0 && additionalTank != 0)
		{
			mainTank++;
			additionalTank--;
		}
	}
	printf("%d", distance);
	return 0;
}
*/
